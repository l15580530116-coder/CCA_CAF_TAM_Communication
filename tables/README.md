# Tables

Key processed tables (DEG results, ssGSEA scores, CellChat LR pairs, enrichment results, etc.) are available from the corresponding authors upon reasonable request and will be deposited in a public repository upon manuscript acceptance.

Individual scripts in `../scripts/` write output tables to a `tables/` directory.
